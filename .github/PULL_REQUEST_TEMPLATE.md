Pull request creation is restricted to collaborators. An external account cannot
open one, so this template is for a collaborator. Link the issue that agrees the
change first, and keep the diff inside the scope that issue states. A change
outside that scope belongs in its own issue.
